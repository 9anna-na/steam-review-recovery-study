# Synthetic Stata fixture

`steam_reviews_synthetic.csv` is generated by
`../../scripts/make_synthetic_fixture.py` using deterministic rules and no Steam
review records. It mirrors only the fields and date coverage required to test
the Stata workflow.

The fixture is suitable for code demonstrations. Its estimates are artificial
and must never be reported as findings about Steam users or the localization
episode.
