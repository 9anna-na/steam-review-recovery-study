# Output boundary

The files directly under `tables/`, `figures/`, and `logs/` are safe aggregate artifacts from the licensed Stata 16.1 formal run on 10 September 2026. They contain no usernames, review text, review IDs, or review-level rows.

Outputs under `synthetic/` come from the entirely artificial public fixture. They verify that the workflow executes, but they must not be cited as evidence about Steam users.

`figure_02_placebo_windows.png` was visually reviewed during the formal run. Its rightmost date label is clipped in the current export. The underlying table and estimates are retained, and `../02_redraw_placebo_figure.do` provides a graph-only formatting refresh that does not require private review-level data.
